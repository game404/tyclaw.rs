//! 沙箱类型重导出（trait 定义已移至 tyclaw-tool-abi）。

pub use tyclaw_tool_abi::{
    PathMount, Sandbox, SandboxDirEntry, SandboxExecResult, SandboxFileStat, SandboxGlobEntry,
    SandboxGrepRequest, SandboxGrepResponse, SandboxPool, SandboxWalkEntry,
};
